# ---- Postgres ----
POSTGRES_USER=erp
POSTGRES_PASSWORD=erp
POSTGRES_DB=erp
POSTGRES_PORT=5432

# ---- Redis ----
REDIS_PORT=6379

# ---- API (apps/api) ----
API_PORT=3001
NODE_ENV=development
# Usado fora do docker-compose (localhost); dentro do compose a API monta a URL com os valores acima.
DATABASE_URL=postgresql://erp:erp@localhost:5432/erp
REDIS_URL=redis://localhost:6379

# JWT de staff (painel admin) — gere segredos fortes em produção (ex: openssl rand -hex 64)
JWT_ACCESS_SECRET=troque-este-segredo-de-acesso
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=troque-este-segredo-de-refresh
JWT_REFRESH_EXPIRES_IN=7d

# JWT de cliente (loja virtual) — segredos DIFERENTES dos de staff acima, de propósito:
# um token de cliente nunca pode ser válido como token de equipe, e vice-versa.
CUSTOMER_JWT_ACCESS_SECRET=troque-este-segredo-de-acesso-cliente
CUSTOMER_JWT_ACCESS_EXPIRES_IN=30m
CUSTOMER_JWT_REFRESH_SECRET=troque-este-segredo-de-refresh-cliente
CUSTOMER_JWT_REFRESH_EXPIRES_IN=30d

BCRYPT_SALT_ROUNDS=12

# Nome exibido nos apps autenticadores (Google Authenticator etc.) ao ativar 2FA
TOTP_ISSUER=ComercioERP

# Header usado para resolver o tenant em desenvolvimento (em produção: subdomínio)
TENANT_HEADER=x-tenant-slug

CORS_ORIGIN=http://localhost:3000
STOREFRONT_CORS_ORIGIN=http://localhost:3002

# ComerciON IA (assistente interno, /ai) — opcional. Sem nenhuma chave
# configurada, AI_PROVIDER cai em "stub" (avisa que a IA não está
# configurada, sem quebrar o resto do sistema). Preencha uma das duas
# chaves abaixo e troque AI_PROVIDER pra "anthropic" ou "openai" pra
# ativar de verdade — dá pra trocar de provedor só mudando essa variável.
AI_PROVIDER=stub
# AI_PROVIDER=anthropic
# ANTHROPIC_API_KEY=
# AI_PROVIDER=openai
# OPENAI_API_KEY=

# ---- Web (apps/web — painel admin) ----
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3001
# Usada para montar o link público do orçamento (loja virtual) mostrado ao staff
NEXT_PUBLIC_STOREFRONT_URL=http://localhost:3002

# ---- Storefront (apps/storefront — loja virtual) ----
STOREFRONT_PORT=3002
NEXT_PUBLIC_STOREFRONT_API_URL=http://localhost:3001
